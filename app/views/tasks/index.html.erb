<h1>List of tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task[:title], task_path(task.id) %>
    <%= link_to task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Delete #{task.title}?"} do %> <span>"delete"</span>
    </li>
    <%# <li><%= link_to task.title, tasks_path(@task) </li> %>
    <% end %>
  <% end %>
</ul>

<p> <%= link_to "Add a task", new_task_path %> </p>
<%# <p><%= link_to "Delete a task", destroy_task_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} </p> %>
